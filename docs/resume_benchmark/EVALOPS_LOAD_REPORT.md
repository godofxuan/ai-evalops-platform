# AI EvalOps load report

Status: `PENDING`.

Source of truth will be the hash-verified final bundle under `docs/results/load/<run-id>/final/` and
the normalized rows in `LOAD_RESULTS.csv`. No result is reported before that bundle exists.
