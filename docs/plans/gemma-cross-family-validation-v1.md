# Gemma跨家族续测计划（2026-09-12）

用户确认下载完成或取得结论后继续上一轮工作。本轮只拉取官方 `gemma4:e2b-it-qat`，不新增第二款模型或付费API。沿用 `codex/public-benchmark-validation-v1`，保留上一轮ZIP/结果/代码基线，不修改main或RAG，也不重跑旧Qwen模型。

## 顺序和预先限制

1. 确认官方具体tag（约4.3GB）及本机磁盘余量，正常拉取，记录完整digest和能力。已有Ollama0.34.0，不自动升级运行时。
2. 接入只增加该精确本地标签；保持原默认三模型，不因安装新模型自动扩大旧命令的调用量。使用TDD验证Gemma执行→落盘→离线校验以及CLI计划生成，仍拒绝云模型、未批准标签和旧账本篡改。
3. 用两条非基准的简单文本/JSON合成问题做兼容性预检，不将其当质量测评。明确关闭thinking，除非运行时不支持而应记录阻塞。不是根据模型答题分数调提示词。
4. BFCL同100题、RAGBench同60题、相同prompt/seed/options/context budget。Gemma单独新计划与账本。预计160计划项、158真实调用和同2项上下文阻塞；实际以日志为准。遇到错误照实保留，不重试挑最好值，不扩大上下文让新模型单独占优。
5. 增加跨运行的只读比较：验证两份原始报告与账本后，核对来源/题目/参考/消息/参数一致；保留不同执行时间和硬件非独占限制。不能伪造一个包含旧请求的新执行计划。比较结果为探索性，不是官方榜单或正式业务A/B。
6. 回归、离线重算、写新增变化与负面结果；生成新的交付扩展包，保留上一轮包及首版交付失败记录。达到本轮边界即停止，不继续堆新模型。

## 判断规则

Gemma官方推荐采样参数与我们已冻结的低随机配置不同。本轮为了固定对照，沿用原配置，不声称Gemma的最优能力；如果系统角色/JSON schema/think参数不兼容，先区分接入故障与模型答题错误。下载失败则记录失败、完成可离线部分，不冒称已跑。

成果是跨家族对照和可审计实验方法，不保证模型分数提高。公开RAGBench为自动标注且60题只有9个负例；误放行、误拒绝、balanced accuracy和分母必须同时呈现。不得称人评通过、生产可用或硬件速度提升。

官方来源：https://ollama.com/library/gemma4:e2b-it-qat 、 https://docs.ollama.com/api/pull 。
